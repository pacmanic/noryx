services:
  postgres:
    image: postgres:18-alpine
    container_name: noryx-db
    environment:
      POSTGRES_DB: noryx
      POSTGRES_USER: noryx
      POSTGRES_PASSWORD: noryx
    ports:
      - "5432:5432"
    volumes:
      - noryx-db-data:/var/lib/postgresql

volumes:
  noryx-db-data:
